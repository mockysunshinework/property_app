#!/bin/bash -e

rm -f /rails/tmp/pids/server.pid

if [ "$1" = "bundle" ] && [ "$2" = "exec" ] && [ "$3" = "rails" ] && [ "$4" = "server" ]; then
  ./bin/rails db:migrate
  # ./bin/rails db:seed # adminデータを投入する時だけコメントアウトを不活性にする。
fi

exec "${@}"
